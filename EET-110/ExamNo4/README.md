# ExamNo4

Path: `EET-110/ExamNo4`

## Items

- `Debug/` (folder)
- `AssemblyInfo.cpp` (file)
- `ExamNo4.cpp` (file)
- `ExamNo4.vcproj` (file)
- `ReadMe.txt` (file)
- `resource.h` (file)
- `stdafx.cpp` (file)
- `stdafx.h` (file)

## Notes

This directory was indexed from the old Portafolio Drive source. Large binaries and archives may remain Drive-only.
