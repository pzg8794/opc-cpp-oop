# AnotherCurrection

Path: `EET-110/Visual Studio 2008/Projects/AnotherCurrection/AnotherCurrection`

## Purpose

Indexed project directory from the old Portafolio cleanup.

## Items

- `AnotherCurrection.cpp` (file)
- `AnotherCurrection.vcproj` (file)
- `AssemblyInfo.cpp` (file)
- `ReadMe.txt` (file)
- `resource.h` (file)
- `stdafx.cpp` (file)
- `stdafx.h` (file)

## Notes

README generated during the old Portafolio cleanup. Use the root README and `OVERLEAF_PREP.md` for source and conversion context.
