# ShirtDiscount_PG

Path: `CppProjects/ShirtDiscount_PG/ShirtDiscount_PG`

## Purpose

Indexed project directory from the old Portafolio cleanup.

## Items

- `AssemblyInfo.cpp` (file)
- `Form1.h` (file)
- `ReadMe.txt` (file)
- `resource.h` (file)
- `ShirtDiscount_PG.cpp` (file)
- `ShirtDiscount_PG.vcproj` (file)
- `stdafx.cpp` (file)
- `stdafx.h` (file)

## Notes

README generated during the old Portafolio cleanup. Use the root README and `OVERLEAF_PREP.md` for source and conversion context.
